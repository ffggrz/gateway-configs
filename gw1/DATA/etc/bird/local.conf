# lokale Parameter (verschieden für jedes Gateway unserer Community)

router id 10.207.0.181;                         # Router-Id (entspricht üblicherweise v4-IP-Adresse)

define addr_ic_v4 = 10.207.0.181;               # eigene Gateway-Adresse im IC-VPN (Tinc-Tunnel)
define addr_br_v4 = 10.181.0.11;                # eigene Mesh-Bridge-IP
define net_br_v4 = 10.181.0.11/32;              # eigene Mesh-Bridge-IP (als Netzdefinition)
define addr_neighbor1_v4 = 10.181.0.12;         # interner Peer 1
define addr_neighbor2_v4 = 10.181.0.13;         # interner Peer 2
define pool_dhcp1_v4 = 10.181.1.0/24;           # Adresse DHCP-Pool 1 (/24) für Clients an diesem Gateway
define pool_dhcp2_v4 = 10.181.2.0/24;           # Adresse DHCP-Pool 2 (/24) für Clients an diesem Gateway
define pool_dhcp3_v4 = 10.181.21.0/24;          # Adresse DHCP-Pool 3 (/24) für Clients an diesem Gateway
define pool_dhcp4_v4 = 10.181.22.0/24;          # Adresse DHCP-Pool 4 (/24) für Clients an diesem Gateway
define ffrl_nat_address_v4 = 185.66.194.27;     # externe IPv4-Adresse beim Rheinland-Backbone
define ffrl_nat_net_v4 = 185.66.194.27/32;      # externe IPv4-Adresse beim Rheinland-Backbone (als netzdefinition)

define addr_ic_v6 = fec0::a:cf:0:b5;            # eigene Gateway-Adresse im IC-VPN (Tinc-Tunnel)
define addr_br_icvpn_v6 = fdb5:78b:64cc::11;    # eigene Mesh-Bridge-IP von ICVPN
define addr_br_ffrl_v6 = 2a03:2260:100b::11;    # eigene Mesh-Bridge-IP vom FFRL-Backbone
define addr_neighbor1_v6 = fdb5:78b:64cc::12;   # interner Peer 1
define addr_neighbor2_v6 = fdb5:78b:64cc::13;   # interner Peer 2
